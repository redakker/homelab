version: "3.7"

services:
  nextcloud:
    image: nextcloud
    container_name: nextcloud
    hostname: nextcloud
    ports:
      - 8888:80/tcp
      - 8888:80/udp
    volumes:
      - ./nextcloud:/var/www/html
    restart: always